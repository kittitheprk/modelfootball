# ⚽ Football Match Prediction System — Simulator v9

ระบบวิเคราะห์และทำนายผลฟุตบอลแบบอัตโนมัติ ใช้ข้อมูลสถิติจากหลายแหล่ง + AI เพื่อคำนวณความน่าจะเป็นของผลการแข่งขัน

## ✨ ความสามารถหลัก

| ฟีเจอร์ | รายละเอียด |
|---|---|
| **Poisson + Dixon-Coles** | โมเดลทางสถิติสำหรับคำนวณสกอร์และผล 1X2 |
| **Dynamic Lineup Strength** | ปรับกำลังทีมตามรายชื่อนักเตะจริง/คาดการณ์ |
| **OPTA Advanced Stats** | สถิติขั้นสูงระดับผู้เล่น (Attacking, Passing, Defending, Carrying, GK) |
| **Key Matchups** | วิเคราะห์การดวลตามตำแหน่ง (ริมเส้น/กลาง/หน้าประตู) |
| **Tactical Scenarios** | สร้างฉากเกมเชิงแท็กติก พร้อมโอกาสเกิดและโอกาสนำไปสู่ประตู |
| **Fatigue Analysis** | วิเคราะห์ความเหนื่อยล้าจากความถี่การลงแข่ง |
| **xG / xT Engine** | คำนวณ Expected Goals + Expected Threat จาก Match Logs |
| **Auto Calibration** | ปรับค่าโมเดลอัตโนมัติจากผลจริงย้อนหลัง |
| **AI Report** | สร้างรายงานวิเคราะห์ภาษาไทยผ่าน Gemini API |

## 📁 โครงสร้างโปรเจกต์

```
.
├── analyze_match.py           # 🎯 สคริปต์หลักสำหรับวิเคราะห์แมตช์
├── simulator_v9.py            # 🧠 โมเดล (Poisson + lineup + matchup + fatigue)
├── xg_engine.py               # 📊 Rolling xG/xGA + form
├── update_tracker.py          # 📋 บันทึก prediction ลง Excel
├── update_football_data.py    # 🔄 Pipeline อัปเดตข้อมูลทั้งหมด
├── scrape_stats_opta.py       # 🌐 Scraper สำหรับ OPTA stats
│
├── match_context.txt          # 📝 ข้อมูลก่อนแข่ง (ไลน์อัป/ข่าวทีม)
├── latest_prediction.json     # 📄 ผลทำนายล่าสุด
├── model_calibration.json     # ⚙️ ค่าปรับโมเดลจากผลจริง
├── model_performance.json     # 📈 ตัวชี้วัดคุณภาพโมเดล
├── prediction_tracker.xlsx    # 📊 ตารางบันทึกผลวิเคราะห์
│
├── Match Logs/                # ข้อมูลแมตช์ย้อนหลัง
├── sofaplayer/                # สถิติผู้เล่นรายทีม
├── position/                  # ตำแหน่งผู้เล่น
├── player_characteristics/    # จุดเด่น/จุดอ่อนผู้เล่น
├── all stats/                 # ตารางคะแนนและสถิติลีก
├── game flow/                 # เมตริก flow ของแต่ละทีม
├── output_opta/               # สถิติ OPTA ที่ scrape มาแล้ว
├── analyses/                  # รายงานวิเคราะห์ AI
└── tests/                     # ชุดทดสอบระบบ
```

## 🚀 วิธีใช้งาน

### 1. ติดตั้ง

```bash
pip install pandas numpy scipy requests openpyxl selenium
```

### 2. ตั้งค่า API Key (สำหรับรายงาน AI)

```powershell
$env:GEMINI_API_KEY = "your_key"
```

หรือใส่ key ใน `gemini_key.txt` — ถ้าไม่ตั้งค่า ระบบยังทำนายได้แต่ไม่สร้างรายงาน

### 3. อัปเดตข้อมูล

```bash
python update_football_data.py --headless
```

### 4. เตรียม Context ก่อนแข่ง

ก่อนรันวิเคราะห์คู่ใหม่ แนะนำเช็กตามไฟล์ `docs/pre_match_checklist.md` (ใช้เวลา ~30 วินาที)

แก้ไข `match_context.txt`:

```
Match: Arsenal vs Liverpool
Date: 2026-02-19
League: Premier League

**Confirmed Lineups:**
**Arsenal (4-3-3):**
* GK: Raya
* DEF: White, Saliba, Gabriel, Timber
* MID: Odegaard, Rice, Merino
* FW: Saka, Havertz, Martinelli

**Liverpool (4-3-3):**
* GK: Alisson
...
```

### 5. วิเคราะห์แมตช์

```bash
python analyze_match.py Arsenal Liverpool
```

### 6. บันทึกผลลง Tracker

```bash
python update_tracker.py save
```

## 📋 Prediction Tracker

ตารางบันทึกผลแบบเรียบง่าย (`prediction_tracker.xlsx`):

| คอลัมน์ | ความหมาย |
|---|---|
| `Date` | วันที่แข่ง |
| `League` | ลีก |
| `Home` / `Away` | ทีมเหย้า / ทีมเยือน |
| `Home%` / `Draw%` / `Away%` | ความน่าจะเป็น (%) |
| `Pred_Score` | สกอร์ที่ทำนาย |
| `Pred_Result` | ผลที่ทำนาย (Home/Draw/Away) |
| `xG_Home` / `xG_Away` | Expected Goals |
| `Actual_Score` | สกอร์จริง (กรอกหลังแข่ง) |
| `Actual_Result` | ผลจริง (กรอกหลังแข่ง) |
| `Correct` | ทายถูก? (1/0) |
| `Notes` | หมายเหตุ |

## 🔧 คำสั่งที่ใช้บ่อย

```bash
# วิเคราะห์แมตช์
python analyze_match.py <Home> <Away>

# บันทึก prediction ลง tracker
python update_tracker.py save

# อัปเดต Summary หลังกรอกผลจริง
python update_tracker.py

# สร้าง calibration จากผลจริง
python update_tracker.py calibrate

# ประเมินคุณภาพโมเดล
python update_tracker.py evaluate

# ปิดลูป (calibrate + evaluate) อัตโนมัติ
python update_tracker.py close_loop

# ตรวจสุขภาพระบบ
python scripts/system_check.py

# อัปเดตข้อมูลทั้งหมด
python update_football_data.py --headless
```

## ✅ Quick Checklist (30 วินาที)

ดูเวอร์ชันเต็มที่ `docs/pre_match_checklist.md`

```bash
# 1) ตรวจว่า context เป็นคู่ใหม่แล้ว
cat match_context.txt

# 2) เช็ก pipeline ขั้นต้น
python update_football_data.py --headless --preflight-only

# 3) รันวิเคราะห์คู่ถัดไป
python analyze_match.py <Home> <Away>
```

## 📄 Output ที่ได้

| ไฟล์ | รายละเอียด |
|---|---|
| `latest_prediction.json` | ผลทำนาย: ความน่าจะเป็น, สกอร์, xG, Tactical Scenarios |
| `analyses/analysis_*.md` | รายงาน AI (ถ้ามี Gemini key) |
| `prediction_tracker.xlsx` | ตารางบันทึกและติดตามผล |
| `model_calibration.json` | ค่าปรับ global/league/team |
| `model_performance.json` | Brier score, Log-loss, MAE, Quality Gates |

## 🧪 ทดสอบ

```bash
python -m unittest tests/test_automation_paths.py tests/test_full_system.py tests/test_simulator_v9.py
```

## ⚠️ หมายเหตุ

- ใช้ชื่อทีมให้ตรงกับชื่อใน dataset เพื่อ match ได้ถูกต้อง
- Pipeline scraping พึ่งพาโครงสร้างเว็บภายนอก — ถ้าเว็บเปลี่ยนอาจต้องปรับสคริปต์
- ผลลัพธ์เป็น **ความน่าจะเป็น** ไม่ใช่การการันตีผลแข่งขัน
