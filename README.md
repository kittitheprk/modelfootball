# Football Match Prediction & Analysis System (ระบบวิเคราะห์และทำนายผลฟุตบอล)

ระบบนี้เป็นเครื่องมือ AI สำหรับวิเคราะห์และทำนายผลการแข่งขันฟุตบอล โดยใช้ข้อมูลสถิติเชิงลึก (xG, Goals/90, Possession, PPDA) ร่วมกับการจำลองสถานการณ์ (Monte Carlo Simulation) และใช้ Google Gemini AI ในการเขียนรายงานการวิเคราะห์

## 📂 โครงสร้างโปรเจกต์ (Project Structure)

### 1. Core Analysis & Simulation (ส่วนวิเคราะห์และจำลองผล)
*   **`analyze_match.py`**: สคริปต์หลักสำหรับวิเคราะห์การแข่งขัน
    *   รวบรวมข้อมูลทีมและผู้เล่น
    *   รัน Simulator
    *   ส่ง Prompt ไปยัง Gemini AI เพื่อเขียนบทวิเคราะห์
    *   บันทึกผลเป็นไฟล์ `.md` และ `latest_prediction.json`
*   **`simulator_v7.py`**: ระบบจำลองการแข่งขัน v7.0 (Winner Mentality Edition)
    *   จำลองแมตช์ 10,000 ครั้ง
    *   คำนวณโอกาสแพ้-ชนะ-เสมอ
    *   มีระบบโบนัสสำหรับทีมที่เหนือกว่ามาก (Winner Mentality)
*   **`xg_engine.py`**: เครื่องมือคำนวณค่า Expected Goals (xG) และ xGA แบบ Rolling Average (ค่าเฉลี่ยย้อนหลัง)
*   **`match_context.txt`**: ไฟล์สำหรับใส่ข้อมูลสด (Live Context) เช่น รายชื่อผู้เล่นตัวจริง, ข่าวอาการบาดเจ็บ เพื่อให้ AI นำไปประกอบการวิเคราะห์

### 2. Data Management (ส่วนจัดการข้อมูล)
*   **`update_football_data.py`**: สคริปต์สำหรับอัปเดตข้อมูลฟุตบอลทั้งหมด
*   **`scrape_sofaplayer.py`**: ดึงข้อมูลสถิติผู้เล่นรายบุคคล
*   **`scrape_heatmaps.py`**: ดึงข้อมูล Heatmap ของผู้เล่น
*   **`scrape_player_positions.py`**: ดึงข้อมูลตำแหน่งผู้เล่น
*   **`update_tracker.py`**: ระบบติดตามผลการทำนาย (บันทึกลง Excel)

### 3. Visualization & Reporting (ส่วนแสดงผล)
*   **`visualizer.py`**: สคริปต์สร้างกราฟและแผนภาพ (เช่น Radar Chart)
*   **`dashboard/`**: โฟลเดอร์เก็บไฟล์ Dashboard
*   **`output_charts/`**: โฟลเดอร์เก็บรูปภาพกราฟที่สร้างขึ้น
*   **`prediction_tracker.xlsx`**: ไฟล์ Excel บันทึกประวัติการทายผลและความแม่นยำ

---

## 🚀 วิธีการใช้งาน (How to Use)

### 1. การวิเคราะห์แมตช์ (Analyze a Match)
รันคำสั่งโดยระบุชื่อทีมเจ้าบ้านและทีมเยือน:

```bash
python analyze_match.py "ชื่อทีมเจ้าบ้าน" "ชื่อทีมเยือน"
```

ตัวอย่าง:
```bash
python analyze_match.py "Arsenal" "Liverpool"
```
*ระบบจะสร้างไฟล์ `analysis_Arsenal_Liverpool.md` และอัปเดต `latest_prediction.json`*

### 2. การบันทึกผลการทำนาย (Save Prediction)
หลังจากรันการวิเคราะห์แล้ว ให้บันทึกผลลงใน Tracker:

```bash
python update_tracker.py save
```
*หรือดับเบิ้ลคลิกไฟล์ `savepredic.bat`*

### 3. การอัปเดตข้อมูล (Update Data)
เพื่อให้แม่นยำที่สุด ควรอัปเดตข้อมูลก่อนวิเคราะห์:
```bash
python update_football_data.py
```

---

## 🛠️ การติดตั้ง (Installation)

1.  ติดตั้ง Python 3.x
2.  ติดตั้ง Library ที่จำเป็น:
    ```bash
    pip install pandas numpy requests openpyxl xlsxwriter
    ```
3.  ตั้งค่า API Key ของ Google Gemini ในไฟล์ `analyze_match.py` (ตัวแปร `API_KEY`)

---

## 📊 ฟีเจอร์เด่น (Key Features)
*   **Dual Data Sources:** ใช้ข้อมูลผสมผสานทั้งจาก SofaScore (Real stats) และค่า xG (Expected Goals)
*   **Winner Mentality Logic:** Simulator v7.0 มีลอจิกพิเศษให้โบนัสทีมใหญ่ที่มีศักยภาพเหนือกว่าคู่แข่งชัดเจน (>15%)
*   **AI Reporting:** สร้างรายงานภาษาไทยที่อ่านง่าย มืออาชีพ พร้อมฟันธงผลการแข่งขัน
*   **Tracking System:** มีระบบบันทึกและตรวจสอบความแม่นยำของโมเดลอัตโนมัติ
